<source>
  @type forward
</source>

<filter my.app>
  @type parser
  key_name log
  remove_key_name_field true
  reserve_data true
  <parse>
    @type json
  </parse>
</filter>

<match my.app>
  @type elasticsearch
  host elasticsearch
  include_timestamp true
</match>
